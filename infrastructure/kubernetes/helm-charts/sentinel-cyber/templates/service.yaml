apiVersion: v1
kind: Service
metadata:
  name: sentinel-cyber
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: {{ .Values.service.targetPort }} }]
  selector: { app: sentinel-cyber }
