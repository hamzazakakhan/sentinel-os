apiVersion: v1
kind: Service
metadata:
  name: sentinel-ingestion
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: {{ .Values.service.targetPort }} }]
  selector: { app: sentinel-ingestion }
