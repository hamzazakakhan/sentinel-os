apiVersion: v1
kind: Service
metadata:
  name: sentinel-fusion
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: {{ .Values.service.targetPort }} }]
  selector: { app: sentinel-fusion }
